{
  "scripts": {
    "ts:node": "ts-node src/infrastructure/adapters/api-rest/app.ts",
    "clean": "rm -rf build",
    "build": "tsc",
    "start": "node build/src/infrastructure/adapters/api-rest/app.js",
    "dev": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "reflect-metadata": "^0.1.13",
    "tsyringe": "^4.7.0"
  }
}
